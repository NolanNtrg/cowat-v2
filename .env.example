VITE_PORT=3000

# DOCKER
DOCKER_DATABASE_PORT="5432"
DOCKER_DATABASE_NAME="cowat"
DOCKER_DATABASE_USERNAME="cowat"
DOCKER_DATABASE_PASSWORD="cowat"

# BASE URL
VITE_BASE_URL="http://localhost:${VITE_PORT}"

# Use the following environment variables to show the environment name.
VITE_ENV_NAME="LOCAL"
VITE_ENV_EMOJI="🚧"
VITE_ENV_COLOR="gold"

# DATABASE
DATABASE_URL="postgres://${DOCKER_DATABASE_USERNAME}:${DOCKER_DATABASE_PASSWORD}@localhost:${DOCKER_DATABASE_PORT}/${DOCKER_DATABASE_NAME}"

# AUTH
AUTH_SECRET="REPLACE ME" # You can use `npx @better-auth/cli@latest secret` to a generated secret
AUTH_SESSION_EXPIRATION_IN_SECONDS=2592000 # 30 days
AUTH_SESSION_UPDATE_AGE_IN_SECONDS=86400 # 1 day (every 1 day the session expiration is updated)
AUTH_TRUSTED_ORIGINS="cowat-native://,cowat-native://*" # Mobile app scheme for trustedOrigins config
# AUTH_ALLOWED_HOSTS= # Additional hosts allowed to use the auth API (comma-separated)

# EMAILS
EMAIL_SERVER="smtp://username:password@0.0.0.0:1025"
EMAIL_FROM="Cowat <noreply@example.com>"
RESEND_API_KEY="REPLACE ME"

# FIREBASE
# Config served server-side only via /api/firebase-config and /firebase-messaging-sw
# Find these at: Firebase Console > Project Settings > Your apps > Web app
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
# VAPID public key (Firebase Console > Project Settings > Cloud Messaging > Web Push certificates)
FIREBASE_VAPID_PUBLIC_KEY=

# Service account JSON encoded in base64 — needed for server-side sending (firebase-admin)
# Generate at: Firebase Console > Project Settings > Service Accounts > Generate new private key
# Then run: base64 -i path/to/service-account.json | tr -d '\n'
# Not needed in local dev if using: gcloud auth application-default login
FIREBASE_SERVICE_ACCOUNT=

# CRON
CRON_SECRET=REPLACE_ME

# LOGGER
LOGGER_LEVEL="info"
LOGGER_PRETTY="true"
